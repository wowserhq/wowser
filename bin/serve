#!/usr/bin/env node

const Cluster = require('../lib/server/cluster').default;
const ServerConfig = require('../lib/server/config').default;

ServerConfig.verify().then(function() {
  const cluster = new Cluster();
  cluster.start();
});
